{
  "name": "taiwan-day-trading-candidates",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python -m http.server 8765 --bind 127.0.0.1",
    "test": "node --test tests/*.test.mjs && python -m unittest discover -s tests -p \"test_*.py\"",
    "test:js": "node --test tests/*.test.mjs",
    "test:python": "python -m unittest discover -s tests -p \"test_*.py\"",
    "check": "node --test tests/*.test.mjs && python -m unittest discover -s tests -p \"test_*.py\"",
    "data:update": "python scripts/update_stock_data.py && python scripts/enrich_tpex_industry.py && python scripts/enrich_day_trade_eligibility.py && python scripts/validate_stock_data.py",
    "data:validate": "python scripts/validate_stock_data.py"
  }
}
